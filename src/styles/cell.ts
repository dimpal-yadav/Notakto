import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cell: {
    borderWidth: 1,
    borderColor: '#ddd',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff',
    margin: 2,
  },
  cellText: {
    fontWeight: 'bold',
    color: '#333',
  },
});